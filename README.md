# Station-Manager: logging modules

This module provides logging functionality for the Station-Manager project. It includes a custom logger that is
used throughout all the applications to log messages at different levels (debug, info, warning, error, critical).